const HOME = "/";
const MOVIES = "/movies";
const MOVIEDETAIL = `${MOVIES}/:movieId`;
const SIGNIN = "/signin";
const SIGNUP = "/signup";
const routes = { HOME, MOVIES, MOVIEDETAIL, SIGNIN, SIGNUP };
export default routes;
